import React from 'react';

const VolunteerCard = ({post}) => {
  const { thumbnail, PostTitle, description, Category, Location, OrganizerName, organizerEmail, volunteersNeeded, deadline } = post
  return (
    <div>
      
    </div>
  );
};

export default VolunteerCard;